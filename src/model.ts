export default class Model {
    name: string;
    score: number;
    color: string;

    constructor(name: string, score: number, color: string) {
        this.name = name;
        this.score = score;
        this.color = color;
    }
}
